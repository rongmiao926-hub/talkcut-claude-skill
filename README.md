# TalkCut — 口播快剪 🎬

> **让 AI 帮你自动剪掉口播视频中的"说错部分"**
> 🤯 不用再手动一帧帧找错词！
> 🤖 AI 自动识别，你确认一下就搞定！

---

## 👋 你好，创作者！

你是不是也遇到过这些问题：

- 录了 10 分钟口播，中间卡壳了 3 次 😫
- 说了两遍一样的话，不知道该保留哪个 🤔
- "那个""就是""嗯"太多，看着很乱 😓
- 说错了重新说，前面说错的部分得手动剪掉 ✂️

**现在有 TalkCut 帮你解决！**

> **只需 3 步，10 分钟视频 2 分钟搞定！**

## 🎯 核心功能

| 问题 | TalkCut 怎么处理 |
|------|-----------------|
| 说了两遍一样的话 | 自动识别重复句，保留最完整的那一遍 |
| 话说到一半卡住了 | 识别残句，整句标记删除 |
| "那个""就是""嗯"太多 | 标记卡顿词和语气词 |
| 说错了重新说 | 识别纠正重说，删掉前面说错的部分 |
| 中间停顿太久 | 自动检测静音段（≥0.5 秒可自定义） |
| 想顺手配一段发布文案 | **AI 自动生成视频介绍草稿**（标题+正文+标签+摘要） |

## ✨ 特色功能

### 📝 自定义词典支持
- **字幕生成**时可添加专业术语、人名、品牌名等作为热词
- 提高语音识别准确率，避免专业词汇被误识别
- 支持同音字纠错（如"Agent"被识别为"IT"）

### 🧠 自进化机制
- **记住用户偏好**：AI 会学习你的剪辑习惯
- **自动更新规则**：根据反馈持续优化识别算法
- **个性化审核**：静音阈值、语气词处理等都可自定义

### 🎬 完整工作流
- **转录**：火山引擎 API（快速云端）或 Whisper（本地免费）
- **分析**：AI 逐句识别口误、重复、卡顿
- **审核**：可视化网页，精确到字的标记和编辑
- **剪辑**：一键生成成品视频
- **字幕**：可选烧录字幕到视频
- **文案**：自动生成可直接复制使用的视频介绍

## 🖥️ 跨平台支持

TalkCut 对 macOS、Windows 和 Linux 都做了兼容处理：

- **跨平台核心**：所有核心脚本（转录、剪辑）均为 Node.js 实现，不依赖 bash/shell
- **智能路径处理**：FFmpeg 自动处理不同操作系统的文件路径差异
- **硬件加速**：macOS 使用 VideoToolbox，Windows 自动识别 NVENC/QSV/AMF，Linux 使用硬件编码器
- **多语言支持**：支持中文、英文等语言的语音识别

## 🚀 快速开始（5分钟搞定）

### 前置准备

你需要准备好这些东西：

### 📋 准备清单

- ✅ **一台电脑**（Mac/Windows/Linux 都行，别太旧就行）
- ✅ **Claude Code**（AI 助手）—— 如果还没装，[点这里看详细安装指南](https://my.feishu.cn/wiki/TEx9wUx0hiK6KzkgqbOca1fNnqd?from=from_copylink)
- ✅ **一段口播视频**（手机录的、屏幕录制都行，.mp4 或 .mov 格式）

### 💡 重要提醒

> **TalkCut 是一个"AI 小助手"**，不是复杂的软件！
>
> 你不需要学任何技术，只需要：
> 1. 告诉它："帮我剪这个视频"
> 2. 在网页上点点鼠标确认一下
> 3. 它就帮你剪好了！
>
> 就像让一个会剪辑的朋友帮忙一样简单！

### 第一次使用流程（和 Claude 聊 3 次就搞定！）

#### 🎉 第 1 步：安装（只需一次，1分钟）

**打开 Claude Code，直接告诉它：**

```
帮我安装 videocut 环境
```

Claude 会自动：
- 检查你的电脑环境
- 安装需要的 Node.js、FFmpeg
- 询问你选择语音识别方案（火山引擎或 Whisper）
- 完成所有配置

---

#### 🎬 第 2 步：开始剪辑（最关键的一步）

**打开 Claude Code，告诉它你的视频地址：**

```
帮我剪这个口播视频 /Users/你的名字/Downloads/我的视频.mp4
```

或者如果你视频在其他位置：

```
帮我剪这个口播视频 C:\Users\你的名字\Videos\视频.mp4
```

---

#### 📝 第 3 步：在浏览器中确认并剪辑

1. **等待 Claude 处理**：
   - 它会先提取音频
   - 然后把语音转成文字
   - 最后找出所有说错的地方

2. **浏览器会自动打开一个页面**：
   - 你会看到视频的每一个字
   - 橙色底色的字 = AI 觉得应该删掉的
   - 正常的字 = 保留的

3. **简单确认**：
   - 看看 AI 标记得对不对
   - 如果有不对的，用鼠标拖选取消
   - 如果 AI 漏掉的，用鼠标拖选标记删除

4. **点击执行剪辑**：
   - 确认无误后，点击页面顶部的 **"执行剪辑"** 按钮
   - 等待几秒到几十秒
   - 剪好的视频就自动生成啦！

---

### 📽️ 可选：添加字幕（让你的视频更专业）

如果想给视频加上字幕，告诉 Claude：

```
给我的视频添加字幕
```

它会自动：
- 生成字幕文件
- 在浏览器里让你确认
- 烧录字幕到视频上

## 安装（只需做一次）

### 第 1 步：下载 TalkCut

你可以选择任意一种方法：

---

#### 🎯 方法一：下载 ZIP（最简单，推荐新手）

1. **点击下载 ZIP**：
   - 点击这个页面的绿色 **"Code"** 按钮
   - 在弹出的菜单中选择 **"Download ZIP"**
   - 等待下载完成

2. **解压文件**：
   - 找到下载的 `talkcut-claude-skill-main.zip` 文件
   - 右键选择"解压"或在 Mac 上双击解压
   - 你会得到一个名为 `talkcut-claude-skill-main` 的文件夹

3. **放到正确位置**：
   - 把 `talkcut-claude-skill-main` 文件夹复制到：
   - **Mac**: `/Users/你的用户名/.claude/skills/videocut`
   - **Windows**: `C:\Users\你的用户名\.claude\skills\videocut`
   - （如果 `.claude` 文件夹不存在，先创建它）

---

#### 💻 方法二：使用 Git 命令（适合熟悉命令行的用户）

如果你已经安装了 Git，可以用这个方法：

1. **打开终端**：
   - **Mac**：在启动台搜索"终端"（图标是一个黑色盒子）
   - **Windows**：按 Win+R，输入 `cmd`，回车

2. **克隆仓库**：
   ```bash
   git clone https://github.com/amiaoo/talkcut-claude-skill.git ~/.claude/skills/videocut
   ```

3. **等待完成**：
   - 终端会显示下载进度
   - 完成后你就拥有最新版本了

---

### 💡 两种方法对比

| 特点 | 方法一（ZIP） | 方法二（Git） |
|------|-------------|-------------|
| **难度** | 🟢 非常简单 | 🟡 需要懂一点命令 |
| **更新** | 手动下载新版 | 一条命令更新 |
| **网络** | 不需要 Git | 需要 Git 环境 |
| **推荐** | 🔥 新手首选 | 老用户使用 |

### 第 2 步：安装依赖

打开 Claude Code，输入：

```
帮我安装 videocut 环境
```

Claude 会自动帮你安装需要的工具（Node.js、FFmpeg）。

### 第 3 步：选择语音转录方案

安装过程中，Claude 会问你用哪种语音识别方案。两个选择：

| 方案 | 优点 | 缺点 |
|------|------|------|
| **火山引擎 API** | 速度快，识别准，全平台可用 | 需要注册账号，有 20 小时免费额度 |
| **Whisper 本地模型** | 完全免费，不需要联网 | 速度较慢，首次下载模型占 1.5GB 磁盘，**仅支持 macOS（Apple Silicon）** |

**如果你选火山引擎**，需要获取一个 API Key：

1. 打开火山引擎控制台：https://console.volcengine.com/speech/new/setting/activate
2. 点击左侧边栏的 **「开通管理」**
3. 右侧「服务管理」选择 **「小模型」**
4. 找到 **「音视频字幕生成」**，点击旁边的 **「开通」**
5. 开通后，在控制台获取你的 API Key

> 详细图文指南：https://my.feishu.cn/wiki/Gh0MwxHePidsYfkIx7zcvJQynqc?from=from_copylink

**如果你选 Whisper**，Claude 会自动帮你安装，不需要额外操作。

## 🚀 使用方法

### 基础使用

### 1️⃣ 剪口播

打开 Claude Code，把视频文件路径告诉它：

```
/videocut:cut /Users/你的用户名/Downloads/视频.mp4
```

接下来 Claude 会自动：

1. **提取音频** — 从视频中分离出音频
2. **语音转录** — 把语音转成带时间戳的文字（支持热词）
3. **AI 分析** — 找出重复句、残句、卡顿词、静音段
4. **生成审核页面** — 在浏览器中打开一个网页给你确认
5. **补一份视频介绍草稿** — 审核页里可以直接查看和复制

### 2️⃣ 生成字幕（可选）

为视频添加字幕，支持自定义词典纠错：

```
/videocut:subtitle /Users/你的用户名/Downloads/视频.mp4
```

**字幕功能特点：**
- 自动提取音频并上传
- 火山引擎转录（支持热词提高准确率）
- AI 自动校对常见误识别
- 可视化编辑界面
- 一键烧录字幕到视频

#### 📝 如何自定义词典？

如果你想让 AI 更准确地识别专业术语，可以：

1. **查看词典位置**：
   ```
   用户: videocut 字典文件在哪里？
   ```
   AI 会告诉你：`字幕/词典.txt`

2. **编辑词典**：
   ```
   用户: 打开 字幕/词典.txt
   ```
   然后在文件中添加你的术语，每行一个：
   ```
   skills
   Claude
   Agent
   成峰
   剪辑技能
   ```

3. **AI 会自动使用**这些热词提高识别准确率

### 3️⃣ 自定义偏好（可选）

让 AI 记住你的剪辑偏好：

```
/videocut:update
```

**可以设置的偏好：**
- "静音阈值改成 1 秒"
- "保留适量嗯作为过渡"
- "重复句相似度设为 3 个字"

#### 🔧 如何查看和编辑自定义规则？

如果你想查看或修改 AI 的审核规则，可以：

1. **查看用户习惯目录**：
   ```
   用户: videocut 用户习惯文件在哪里？
   ```
   AI 会告诉你：`剪口播/用户习惯/`

2. **具体文件说明**：
   - `1-核心原则.md` - 删前保后原则
   - `3-静音段处理.md` - 静音阈值设置
   - `5-卡顿词.md` - 卡顿词定义
   - 等等...

3. **修改后生效**：修改后，下次使用时会自动应用新规则

### 4️⃣ 首次安装

```
/videocut:install
```

会自动检查并安装所有依赖：
- Node.js 18+
- FFmpeg
- Python 3.8+
- 火山引擎 API Key（可选）

### 📋 审核页面怎么用？

浏览器会自动弹出一个审核页面，你会看到视频的每一个字都排列在页面上：

- **橙色底色的字** = AI 觉得应该删掉的（口误、重复等）
- **红色底色 + 删除线** = 你手动标记要删的
- **正常的字** = 保留不动

**操作方式：**

| 操作 | 效果 |
|------|------|
| 单击某个字 | 跳到那个位置播放，听听看 |
| 拖动鼠标框选一段 | 批量选中（标记删除）或批量取消 |
| 双击某个字 | 选中/取消这一个字 |
| 按空格键 | 播放/暂停 |
| 按左右方向键 | 前后跳 1 秒 |

确认无误后，点击页面顶部的 **「执行剪辑」** 按钮，等待几秒到几十秒（取决于视频长度），剪好的视频就生成了。

## ✨ AI 视频介绍草稿

审核页下方会自动显示一份 **AI 视频介绍草稿**，包含：

- 🎯 **标题**：吸引眼球的钩子
- 📝 **正文**：围绕核心观点展开
- 🏷️ **标签**：4-6 个相关标签
- 📋 **摘要**：3-5 点核心内容

**特点：**
- 不是机械摘要，而是像创作者自己发的帖子
- 直接复制使用，无需二次编辑
- 根据保留内容智能生成，风格匹配原口播

## 🔄 工作原理

```
你的口播视频
    ↓
① 提取音频（FFmpeg）
    ↓
② 语音转文字（火山引擎 API 或本地 Whisper）
    ↓
③ AI 分析：哪些是口误？哪些是重复？（Claude）
    ↓
④ 生成审核网页 + **AI 视频介绍草稿**
    ↓
⑤ 一键剪辑，输出成品视频（FFmpeg）
    ↓
⑥ 可选：生成字幕（带词典纠错）
```

整个过程中，**真正需要你动手的只有第 ④ 步**——在网页上看一眼 AI 标记得对不对，不对的调整一下，然后点按钮。

## 💡 高级功能

### 🔤 自定义词典（字幕功能）

在 `字幕/词典.txt` 中添加专业术语，每行一个：

```
skills
Claude
Agent
成峰
剪辑skills
claude code
```

AI 会自动将这些词作为热词，提高识别准确率，避免专业词汇被误识别。

### 🧠 自进化机制

TalkCut 会记住你的偏好，越用越准：

- **记录用户反馈**：自动分析失败案例
- **更新审核规则**：将经验整合到方法论中
- **个性化配置**：静音阈值、语气词处理等可自定义

只需告诉 AI：
- "刚才失败了，更新一下"
- "以后静音按 1 秒算"
- "保留适量嗯作为过渡"

这些就像和朋友聊天一样，AI 能听懂你的意思！

## 📁 目录结构

```
~/.claude/skills/videocut/
├── 安装/           # 环境安装说明
├── 剪口播/         # 核心功能：转录 + AI 分析 + 审核 + 剪辑
│   ├── scripts/    # 脚本文件
│   │   ├── volcengine_transcribe.js   # 火山引擎转录
│   │   ├── whisper_transcribe.py      # Whisper 本地转录
│   │   ├── generate_subtitles.js      # 生成字幕数据
│   │   ├── generate_review.js         # 生成审核网页 + 视频介绍草稿
│   │   ├── review_server.js           # 审核服务器
│   │   └── cut_video.js               # FFmpeg 剪辑
│   ├── 用户习惯/   # **个性化审核规则**（可自定义）
│   │   ├── 1-核心原则.md              # 删前保后原则
│   │   ├── 2-语气词检测.md            # 嗯啊呃词处理
│   │   ├── 3-静音段处理.md            # 静音阈值设置
│   │   └── ...                        # 其他检测规则
│   └── show-notes.md                  # **视频介绍草稿生成规则**
├── 字幕/           # 字幕生成功能（含词典纠错）
│   ├── 词典.txt                       # **自定义词典**
│   └── scripts/                        # 字幕脚本
└── 自进化/         # **自我进化机制**
    ├── README.md                       # 自进化说明
    └── SKILL.md                        # 记录用户偏好
```

## 🔍 如何查找和编辑文件？

### 查看文件路径的方法

你可以这样问 AI 帮你找到文件：

1. **查看字典位置**：
   ```
   videocut 字典文件在哪里？
   ```
   AI 会告诉你：`字幕/词典.txt`

2. **查看用户习惯目录**：
   ```
   videocut 用户习惯文件在哪里？
   ```
   AI 会告诉你：`剪口播/用户习惯/`

3. **查看所有配置文件**：
   ```
   videocut 的配置文件有哪些？
   ```
   AI 会列出所有重要文件和它们的用途

### 编辑文件的步骤

1. **让 AI 打开文件**：
   ```
   打开 字幕/词典.txt
   ```

2. **添加你的术语**：
   直接告诉 AI 要加什么词：
   ```
   请在词典里添加这些词：Claude Code、AI 剪辑、口播技巧
   ```
   AI 会自动帮你添加到文件里，每行一个

3. **保存文件**：
   ```
   保存
   ```

### 常用文件位置速查

| 功能 | 文件路径 | 用途 |
|------|---------|------|
| **自定义词典** | `字幕/词典.txt` | 提高语音识别准确率 |
| **审核规则** | `剪口播/用户习惯/` | 自定义 AI 审核偏好 |
| **文案规则** | `剪口播/show-notes.md` | 视频介绍草稿生成规则 |
| **自进化记录** | `自进化/` | AI 学习记录 |

## ❓ 常见问题

### Q: 我不会用终端 / 命令行怎么办？

没关系！TalkCut 专门为新手优化过：

**安装时**：
- 使用上面的"下载 ZIP"方法，完全不需要终端

**日常使用**：
- 只需要在 Claude Code 里和它说话就行，比如：
  ```
  帮我剪这个视频 我的视频.mp4
  ```
- 不需要记任何命令，也不需要打开终端

**如果你真的需要打开终端**（仅安装时可能需要）：
1. **Mac**：在启动台搜索"终端"（图标是一个黑色盒子）
2. **Windows**：按 Win+R，输入 `cmd`，回车
3. 复制粘贴命令，按回车就行

### Q: 视频很长，会不会很慢？

转录速度取决于你选的方案：
- **火山引擎**：几分钟内完成（云端处理）
- **Whisper 本地**：大约是视频时长的 1-3 倍（取决于你的电脑性能）

剪辑本身很快，通常几秒到十几秒。

### Q: 审核页面打不开？

试试这几个简单步骤：

1. **检查浏览器**：确保浏览器没有阻止弹窗
2. **刷新页面**：按 F5 或点击刷新按钮
3. **换个浏览器**：试试 Chrome、Edge 或 Safari
4. **关闭其他程序**：如果有其他程序占用 8899 端口，先关掉它们

**如果还是不行**，可以：
- **Mac**：打开"活动监视器"，找到占用 8899 端口的程序，右键"强制退出"
- **Windows**：打开"任务管理器"，找到对应进程，右键"结束任务"

### Q: 可以在 Windows 上用吗？

可以。所有核心脚本已改写为 Node.js，在 Windows 上无需安装额外的 bash 环境。选择火山引擎 API 方案即可，它是云端处理，不挑系统。唯一不能用的是 Whisper 本地模型——它依赖 Apple Silicon 的 MLX 框架，仅限 Mac。

### Q: Linux 支持吗？

部分支持！核心功能（剪口播、字幕生成）完全支持，但 Whisper 本地模型暂不支持。推荐使用火山引擎 API。

### Q: 免费吗？

TalkCut 本身完全免费开源。费用取决于你的选择：
- **使用 Whisper 本地模型**：完全免费
- **使用火山引擎 API**：有 20 小时免费额度，超出后按量计费
- **Claude Code 本身的费用**：需要 Anthropic 账号（有免费额度）

### Q: 字典功能怎么用？

在 `字幕/词典.txt` 文件中添加你的专业术语、人名、品牌名等，每行一个。AI 会在转录时自动将这些词作为热词，提高识别准确率。

### Q: 自进化怎么工作？

当你告诉 AI "记住这个" 或"更新规则"时，它会：
1. 自动回溯上下文，找出问题点
2. 将经验整合到方法论中（不是只加到末尾）
3. 更新用户画像，下次更好地服务你

### Q: 视频介绍草稿质量如何？

AI 会分析保留的内容，生成像创作者自己发的帖子，不是机械摘要。包含标题、正文、标签和摘要，可直接复制使用。

## 🏗️ 技术架构

TalkCut 采用模块化设计，各组件独立：

- **转录引擎**：火山引擎 API（云端）+ Whisper（本地）
- **分析引擎**：Claude AI 语义理解
- **审核界面**：Web-based 实时编辑
- **剪辑引擎**：FFmpeg 高性能处理
- **字幕系统**：支持自定义词典纠错
- **进化系统**：用户偏好持续学习

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 致谢

- **原作**：成峰（公众号「AI 产品自由」）—— 原始项目 [videocut-skills](https://github.com/Ceeon/videocut-skills)
- **当前版本完善**：[Dogtor 大王](https://xhslink.com/m/1GxnHJxjrnd)（小红书）
- **感谢所有贡献者**：让 TalkCut 越来越好用！

## 📄 License

MIT License - 详见 [LICENSE](LICENSE) 文件
